# trigger:
# - master # This can be the branch you want to trigger the pipeline from

# pool:
#   vmImage: 'ubuntu-latest'

# steps:
# - script: npm install
#   displayName: 'Install dependencies'

# - script: npm run build # Build your static web app
#   displayName: 'Build'

# - task: CopyFiles@2
#   inputs:
#     contents: 'build/**' # Specify the build output folder
#     targetFolder: '$(Build.ArtifactStagingDirectory)'
#   displayName: 'Copy build files'

# - task: PublishBuildArtifacts@1
#   inputs:
#     artifactName: 'drop'
#   displayName: 'Publish build artifacts'

name: Azure Static Web Apps CI/CD

on:
  push:
    branches:
      - master


- script: |
    echo "azure_static_web_apps_api_token: {{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_DELIGHTFUL_SMOKE_04EBBC910 }}"
    echo "repo_token: ${{ secrets.GITHUB_TOKEN }}"
  displayName: 'Log Values'

jobs:
  build_and_deploy_job:
    if: github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action != 'closed')
    runs-on: ubuntu-latest
    name: Build and Deploy Job
    steps:
      - uses: actions/checkout@v3
        with:
          submodules: true
      - name: Build And Deploy
        id: builddeploy
        uses: Azure/static-web-apps-deploy@v1
        with:
          azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_DELIGHTFUL_SMOKE_04EBBC910 }}
          repo_token: ${{ secrets.GITHUB_TOKEN }} # Used for Github integrations (i.e. PR comments)
          action: "upload"
          ###### Repository/Build Configurations - These values can be configured to match your app requirements. ######
          # For more information regarding Static Web App workflow configurations, please visit: https://aka.ms/swaworkflowconfig
          app_location: "/portfolio-ui" # App source code path
          output_location: "build" # Built app content directory - optional
          ###### End of Repository/Build Configurations ######
